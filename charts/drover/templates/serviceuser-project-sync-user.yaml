apiVersion: management.cattle.io/v3
kind: User
metadata:
  name: {{ include "drover.projectSync.user" . }}
  labels:
    {{- include "drover.labels" . | nindent 4 }}
username: drover-project-sync
displayName: drover-project-sync
enabled: true
mustChangePassword: false
principalIds:
  - local://{{ include "drover.projectSync.user" . }}
